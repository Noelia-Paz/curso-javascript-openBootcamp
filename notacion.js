//notacion

//; delimita el final de una linea
const b = 4;

//. el punto se utiliza en los objetos para acceder a los atributos ej: movil.anchura

//[] los corchetes se utilizan para las listas , arreglos o arrays
const array = [1, 2, 3, 4];
console.log(array[3]);

//() los parentecis se utilizan para funciones
function suma(a, b) {
  //se escribe la funcion
}

//{} las llaves se usan para objetos , funciones y estructuras de control.
const movil = {
  altura: 5,
  ancho: 2,
};

//ejemplo de estructura de control:
if (true) {
  //todo lo que haya entre llaves
}
